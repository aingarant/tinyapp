<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/_head'); %>
  </head>
  <body>
    <header><%- include('../partials/_header'); %></header>

<main>
      <div class="container">
        <div class="login-box p-4 bg-info w-50 mx-auto text-dark text-center">
          <h3>Login</h3>

          
          <% if(message) { %>
            <div class="mx-auto w-100 p-2 bg-danger text-white"><%- message %></div>
          <%} %>

          <form action="/login" method="post">
            <div class="form-group">
              <input
                type="email"
                name="email"
                placeholder="email"
                class="form-control my-2"
              />
            </div>

            <div class="form-group">
              <input
                type="password"
                name="password"
                placeholder="password"
                class="form-control my-2"
              />
            </div>

            <button class="form-control btn btn-danger my-2">Login</button>
          </form>
          Don't have an account? <a href="/register">Register Here.</a>
        </div>
      </div>
    </main>

    <footer><%- include('../partials/_footer'); %></footer>
  </body>
</html>
